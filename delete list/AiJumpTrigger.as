package  
{
	
	import flash.display.MovieClip;
	
	/////////////////////////////////////////////////////////////////////////////////
	////////////////			DEPRECATED - MARKED FOR DELETION	/////////////////
	/////////////////////////////////////////////////////////////////////////////////
	
	//Works the same as the collision. Every instance pushes itself into an array that is later copied by the XML reader and moved across into a new class
	public class AiJumpTrigger extends MovieClip 
	{
		//public static var jumpArray:Array = new Array();
		
		public function AiJumpTrigger() 
		{
			//jumpArray.push(this);
			//Set to invisible as soon as it's instantiated
			//visible = false;
		}
	}
	
}
